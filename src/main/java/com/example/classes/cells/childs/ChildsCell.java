package com.example.classes.cells.childs;

import com.example.classes.cells.CellMy;

public class ChildsCell extends CellMy {
    public ChildsCell(int strata, int age, int population) {
        super(strata, age, population);
    }
}
